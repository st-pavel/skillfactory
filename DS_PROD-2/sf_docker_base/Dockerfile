# 1. Используем указанный базовый образ
FROM mike0sv/sf_docker_base

# 2. Копируем наш файл внутрь контейнера (например, в корень и называем его так же)
COPY secret.txt /secret.txt

# 3. Создаем переменную окружения DATA_PATH, указывающую на путь к файлу внутри контейнера
ENV DATA_PATH=/secret.txt